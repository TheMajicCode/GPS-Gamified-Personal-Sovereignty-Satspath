import test from'node:test';import assert from'node:assert/strict';import{SqliteSettlementRepository}from'../src/index.js';import type{SettlementRecord,SettlementItemRecord}from'@gps/settlement';const s:SettlementRecord={id:'s',agreementId:'a',grossSat:1,network:'mock',status:'READY_FOR_APPROVAL',maxTotalFeeSat:1,snapshotJson:'{}',createdAt:'now'},i:SettlementItemRecord={id:'i',settlementId:'s',role:'r',recipientDid:'d',recipientName:'n',weight:1,amountSat:1,status:'READY',feeLimitSat:1};test('persists records and enforces payment hash idempotency',()=>{const r=new SqliteSettlementRepository('file::memory:');r.createSettlement(s,[i]);assert.equal(r.getItems('s').length,1);const a={id:'a1',settlementItemId:'i',idempotencyKey:'k',paymentHash:'h',status:'PAYING',createdAt:'n',updatedAt:'n'};assert.equal(r.createAttempt(a),true);assert.equal(r.createAttempt({...a,id:'a2'}),false);r.close()});
