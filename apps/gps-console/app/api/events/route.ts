import{seed,view}from'@/server/runtime';export const dynamic='force-dynamic';export async function GET(){const s=seed(),encoder=new TextEncoder();let timer:ReturnType<typeof setInterval>;const stream=new ReadableStream({start(c){const send=()=>c.enqueue(encoder.encode(`data: ${JSON.stringify(view(s.id))}\n\n`));send();timer=setInterval(send,1500)},cancel(){clearInterval(timer)}});return new Response(stream,{headers:{'Content-Type':'text/event-stream','Cache-Control':'no-cache','Connection':'keep-alive'}})}
